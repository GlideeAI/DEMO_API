name: Deploy Based on Branch

on:
  push:
    branches:
      - main
      - staging
      - dev

jobs:
  deploy:
    name: Deploy Job
    runs-on: ubuntu-latest

    environment:
      name: ${{ github.ref_name }}  # Automatically picks 'main', 'staging', or 'dev'

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Simulate Deployment
        run: echo "🚀 Code pushed to '${{ github.ref_name }}' — deploying to '${{ github.ref_name }}' environment"

      - name: Complete Deployment
        run: echo "✅ Deployment to '${{ github.ref_name }}' finished"
