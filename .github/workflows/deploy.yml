name: Deploy to PROD

on:
  push:
    branches:
      - main  # Only run when code is pushed/merged to 'main'

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest

    environment:
      name: production  # You can change the environment name if needed

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print deploy message
        run: echo "🚀 Code pushed to main branch — will deploy to PRODUCTION environment"
