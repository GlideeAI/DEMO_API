name: Deploy to Production Only

on:
  push:
    branches:
      - main  # 🚨 Only triggers when code is pushed to 'main'

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest

    environment:
      name: production  # Only 'production' environment will be used

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Simulate Deployment
        run: echo "🚀 Code pushed to 'main' — deploying to PRODUCTION environment"

      - name: Complete Deployment
        run: echo "✅ Deployment finished for PRODUCTION"
